---
"@asteby/metacore-ui": patch
---

Column sort menu gains a "Quitar orden" item (shown only when the column is
sorted) that clears the column's sort via `column.clearSorting()`, returning it
to the neutral unsorted (↕) state.
