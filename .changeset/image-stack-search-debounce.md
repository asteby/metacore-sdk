---
"@asteby/metacore-runtime-react": minor
---

Add `image_stack` cell/modal display (logo above label, no muted plate) and debounce DynamicTable / kanban free-text search (~350ms) so typing on large catalogs is less abrupt.
