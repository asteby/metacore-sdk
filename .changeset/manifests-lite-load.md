---
'@asteby/metacore-sdk': minor
---

Shell cold path: `manifestsLite()` prefers `?lite=1` when revalidating the catalog, and `FrontendSpec.load?` is typed for `eager|route|action|idle` so hosts can honor the kernel contract without host-only hacks.
