---
'@asteby/metacore-runtime-react': patch
---

El campo con tipo "icon" (así lo sirve el kernel cuando el manifest declara
widget:"icon") ahora resuelve al selector de íconos en vez de degradar a un
input de texto.
