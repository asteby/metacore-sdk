---
"@asteby/metacore-ui": minor
---

Add `Modal` and `Wizard` primitives (AST-18 Phase 0c). New subpath entries `@asteby/metacore-ui/modal` (`Modal`, `FormModal`) and `@asteby/metacore-ui/wizard` (`Wizard`, wizard context) for building multi-step flows and form dialogs on top of the shared dialog primitives.
