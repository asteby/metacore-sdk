module github.com/asteby/metacore-addon-fiscal-mexico

// WASI module compiled with TinyGo. The host (metacore kernel) invokes the
// exported functions directly over the WASM ABI — no net/http, no server.
go 1.23
