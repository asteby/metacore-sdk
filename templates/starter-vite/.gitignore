# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules
.pnp
.pnp.js

# Build output
dist
dist-ssr
build
*.local
dev-dist

# Editors
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# TS incremental
*.tsbuildinfo
.tmp

# Environment
.env
.env.local
.env.*.local
!.env.example

# Generated TanStack Router tree — COMMITTED to the repo (ecosystem
# convention: metacore-starter / link / ops all commit it) so `tsc -b`
# resolves the ./routeTree.gen import before `vite build` regenerates it.
# Regenerate with: pnpm gen:routes (root) or vite dev/build.
